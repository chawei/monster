<h1>Bites</h1>

<% @bites.each do |bite| %>
<div class="bite">
  <span class=image><%= image_tag bite.normalized_image_url %></span>
  <span>from <%= link_to bite.url, bite.url %></span>
</div>
<% end %>
